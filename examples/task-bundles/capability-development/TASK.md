# 虚构紧固件批次汇总能力交付

只使用公开 Starter 中的虚构数据，构建现有示例能力并生成标准 Task Result。

本任务不签名、不发布、不接触设备，也不声称 Windows HIL 或客户验收已经发生。

`result.example.json` 是固定值的合同示例，不是某次真实执行的交付凭据；实际构建会在
`dist/task-results/` 生成带当前时间、Git 状态和制品摘要的新回执。
