# __DISPLAY_NAME__

这是由 AIOS Capability Pack Starter 生成的 `__CAPABILITY_ID__/__VERSION__` 源目录。

发布前只需要完成四件事：

1. 用工程师能看懂的中文改写 `ui/form.json` 中的能力卡；
2. 实现 `runtime/main.py`，并由 AIOS Core 的固定入口接入它；
3. 把真实输入、确认和结果写进 `schemas/`；
4. 用一个最小真实例子替换 `golden/minimal.json`。

当前占位实现会返回 `NOT_IMPLEMENTED`。不要把未完成的 Starter 发布给设备。

构建命令见仓库根目录的 `docs/capability-quickstart.md`。
