#!/usr/bin/env python3
"""Run the Starter CLI without installing the package."""

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from aios_capability_starter.pack import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
